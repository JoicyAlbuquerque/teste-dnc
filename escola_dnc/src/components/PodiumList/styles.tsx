import { Avatar, Box, styled } from "@mui/material";

export const BoxRow = styled(Box)({
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-around'
});

export const AvatarPodium = styled(Avatar)({
    backgroundColor: '#0D70F2',
});